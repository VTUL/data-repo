<% record.identifier.each do |id| %>
  <% if id.match(/^doi/) && id != t('doi.pending_doi') %>
    <%= link_to id, "https://search.datacite.org/works/#{id}" %>
  <% else %>
    <%= id %>
  <% end %><br />
<% end %>

