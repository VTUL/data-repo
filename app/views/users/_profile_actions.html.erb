    <!-- profile and view buttons -->
      <% if signed_in? %>
        <% if current_user == @user %>
          <%= link_to raw('<i class="glyphicon glyphicon-edit"></i> Edit Your Profile'), sufia.edit_profile_path(@user), class: "btn pull-left subtab" %>
        <% elsif current_user.following?(@user) %>
          <%= link_to raw('<i class="glyphicon glyphicon-eye-close"></i> Unfollow'), sufia.unfollow_profile_path(@user), method: :post, class: "btn pull-left subtab" %>
        <% else %>
          <%= link_to raw('<i class="glyphicon glyphicon-eye-open"></i> Follow'), sufia.follow_profile_path(@user), method: :post, class: "btn pull-left subtab" %>
        <% end %>
      <% end %>